# ── Jargon Vault · Docker 設定 ──────────────────────────────────────
# 複製成 .env.docker 即可(可選;不複製也能跑,會用下面的預設值):
#     cp .env.docker.example .env.docker

# 首次啟動要種什麼資料。改了之後要跑 scripts/reset(清 volume)才會重新套用:
#   sample = demo 帳號 + 範例名詞(試用用,預設)
#   blank  = 只建 demo 帳號、無範例名詞(決定正式使用時選這個)
#   off    = 完全不種(進階;自行註冊帳號)
DEMO_SEED=sample

# Demo 帳號的登入帳密(sample / blank 都會用到)。
# 正式使用建議把密碼改掉,再跑一次 reset。
DEMO_EMAIL=demo@example.com
DEMO_PASSWORD=demo1234

# 之後**每個自行註冊的新帳號**要不要也種一份範例名詞進他自己的庫
# (頁面最上方會有一列「點此刪除所有 demo 資料」,按下就清乾淨)。
# 上面的 DEMO_SEED 只管那個共用的 demo 帳號,兩者互不影響。
#   1(預設)= 種   0 = 不種,新帳號一開始是空的
GLOSSARY_SEED_DEMO=1

# 白名單:只有要「另外註冊新帳號 / 用 Google 首次登入」才需要填(逗號分隔)。
# 沿用 demo 帳號的話留空即可(登入既有帳號不受白名單限制)。
ALLOWED_EMAILS=

# 管理者救援 / 移轉名單(逗號分隔的 email)。兩個用途:
#   1) 即時救援:名單內的帳號一律當 admin,不依賴 users.json——鎖死沒人能管理時的後門。
#   2) 移轉:啟動時會把名單命中的既有帳號永久寫成 store admin。
# 典型情境:站台在「管理」功能加入之前就上線、大家都用 OAuth 登入、沒有任何 admin——
# 把你的登入 email 填進來、docker compose up -d 重啟一次,你的帳號就轉正成 admin,
# 之後可以把這行清空(已永久生效)。
# ADMIN_EMAILS=you@example.com

# ── 以下皆為選填 ────────────────────────────────────────────────────

# session 簽章金鑰。留空會自動產生並存到 volume(/data/.session_secret),重啟不失效。
# SESSION_SECRET=

# 部署在反向代理 / Cloudflare Tunnel 後面時的對外網址(結尾不要帶 /)。
# PUBLIC_BASE_URL=https://jargon.example.com

# 要用 Google 登入才需要(Google Cloud Console 申請 OAuth Client;
# Authorized redirect URI 填 <PUBLIC_BASE_URL>/api/auth/google/callback)。
# GOOGLE_CLIENT_ID=
# GOOGLE_CLIENT_SECRET=

# 想換對外埠(容器內固定 8787)可在 shell 或根目錄 .env 設 HOST_PORT,例:
#   HOST_PORT=9000 docker compose up -d
